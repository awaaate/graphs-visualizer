import { createGraph } from "./graph";
import { Graph } from "././graph_types";

function makeGrid(parent:HTMLElement, graph: Graph, columns: number) {
    const rows = Math.floor(graph.nodeList.length  / columns)
    for(let idx in graph.nodeList){
        //check if is in the row

    }

}
function printNode(node: Node) {}
